---
label: SINGLE WORK ITEM
hide_body: true
fields:
- name: layout
  type: text
  config:
    required: false
  label: layout
- name: title
  type: text
  config:
    required: false
  label: title
- name: season
  type: number
  label: season
  description: ''
  default: 0
  required: true
  config:
    min: 
    max: 
    step: 
- name: category
  type: text
  config:
    required: false
  label: category
- name: overlay-menu-info
  type: blocks
  label: Overlay Menu Info
  template_types:
  - overlay-menu-info
  config:
    min: 
    max: 
- name: project-details
  type: blocks
  label: Project Details
  template_types:
  - project-details
  config:
    min: 
    max: 
- name: single-work-item
  type: blocks
  label: SINGLE WORK ITEM
  template_types:
  - meta
  - overlay-menu-info
  - project-details
  - editorial-image-ok
  - project-links
  config:
    min: 
    max: 
pages:
- _catalog/TESTING MY BLOCKS.md
